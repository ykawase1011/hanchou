schema_version = 1
name = "editor"
description = "Perform the final Japanese or user-facing prose review and return an approved durable artifact."
nickname_candidates = ["editor", "copyeditor", "final-reviewer"]
category = "leaf"
may_spawn_herdr_agents = false
max_delegation_depth = 0
skills = ["hanchou-editor", "hanchou-relay"]

[codex]
model = "gpt-5.6-terra"
reasoning_effort = "high"

[claude]
enabled = false
model = "sonnet"
permission_mode = "plan"
tools = ["Read", "Grep", "Glob", "Skill"]
max_turns = 60
color = "yellow"
