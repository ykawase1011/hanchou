schema_version = 1
name = "orchestrator"
description = "Top-level user-facing orchestrator and control-plane owner. Respond quickly, manage Beads and schedules, delegate substantive work through Herdr, and return concise outcomes."
nickname_candidates = ["orchestrator"]
category = "control-plane"
may_spawn_herdr_agents = true
max_delegation_depth = 2
skills = ["herdr", "hanchou-cli", "hanchou-orchestrator", "hanchou-task", "hanchou-schedule", "hanchou-relay", "hanchou-reporting"]

[codex]
model = "gpt-5.6-sol"
reasoning_effort = "high"

[claude]
model = "opus"
permission_mode = "default"
tools = ["Read", "Grep", "Glob", "Bash", "Skill", "Agent"]
max_turns = 80
color = "purple"
