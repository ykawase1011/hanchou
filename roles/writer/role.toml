schema_version = 1
name = "writer"
description = "Draft, rewrite, or polish one bounded Japanese, technical, business, or user-facing document and return a durable final artifact."
nickname_candidates = ["writer", "drafter", "copywriter"]
category = "leaf"
may_spawn_herdr_agents = false
max_delegation_depth = 0
skills = ["hanchou-writer", "hanchou-relay"]

[codex]
model = "gpt-5.6-terra"
reasoning_effort = "high"

[claude]
enabled = false
model = "sonnet"
permission_mode = "plan"
tools = ["Read", "Grep", "Glob", "Skill"]
max_turns = 60
color = "yellow"
