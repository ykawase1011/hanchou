# Hanchou

Read `AGENTS.md`. Canonical roles live under `roles/`; run
`./bin/hanchou render-agents` rather than editing generated role files directly.
Claude is normally used as an Opus mission lead or a Sonnet leaf. Japanese final
prose must receive a Codex editor pass before user delivery.
